export enum StorageKey {
    TOKEN = 'accessToken',
    REFRESH_TOKEN = 'refreshToken',
    EXPIRES = 'expires',
    USER = 'user',
    // EXPIRES_IN = 'expiryIn',
    // TOKEN_TYPE = 'tokenType',
    // LOCALE = 'locale',
    // NEWEST_SIGN_IN = 'newest_sign_in'
}